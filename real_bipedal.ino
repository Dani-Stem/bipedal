#include <Servo.h>  Servo foot_left;  Servo foot_right;  Servo hip_right;  Servo hip_left;   const int y_axis_pin = A1; const int x_axis_pin = A0; const int joyButton = 2;  int joyValue = 0; int sel_state = 0;   int pos_foot_left = 75;    int pos_foot_right = 75;   int pos_hip_right = 75;    int pos_hip_left = 75;   int y_axis; int x_axis; unsigned long lastButtonPressTime = 0; unsigned long debounceDelay = 200; unsigned long lastSerialTime = 0; unsigned long serialInterval = 500;  void setup() {    Serial.begin(9600);     foot_left.attach(6);    foot_right.attach(3);    hip_right.attach(11);    hip_left.attach(9);    pinMode(joyButton, INPUT_PULLUP);  }  void loop() {      int buttonState = digitalRead(joyButton);      foot_left.write(pos_foot_left);      foot_right.write(pos_foot_right);     hip_right.write(pos_hip_right);     hip_left.write(pos_hip_left);         y_axis = analogRead(y_axis_pin);        x_axis = analogRead(x_axis_pin);        if (buttonState == LOW && (millis() - lastButtonPressTime > debounceDelay)) {     if (sel_state != 3){     sel_state = sel_state + 1;      // Serial.println("10");    }     else{     sel_state = 0;     // Serial.println("11");    }      lastButtonPressTime = millis();   }   if (sel_state == 3){     //dance     // Serial.println("9");   }      if (millis() - lastSerialTime > serialInterval) {         Serial.print("Select:");     Serial.print(sel_state);        Serial.print(" x:" );       Serial.print(x_axis);      Serial.print(" y:" );      Serial.println(y_axis);      delay(100);          // Reset the serial timer     lastSerialTime = millis();   }  //right side settings if (sel_state == 1){   //rfmin   if (x_axis < 100){     pos_foot_right = 0;     // Serial.println("1");    }   //rfmax   if (x_axis > 800){     pos_foot_right = 180;     // Serial.println("7");    }   if (x_axis > 100 && x_axis < 800 ){     pos_foot_right = 75;   }    //rhmin   if (y_axis < 100){     pos_hip_right = 0;     // Serial.println("3");    }   //rhmax   if (y_axis > 800){     pos_hip_right = 180;     // Serial.println("5");    }   if (y_axis < 800 && y_axis > 100){     pos_hip_right = 75;   }   }  //left side settings if (sel_state == 0){   //lhmin   if (y_axis < 100){     pos_hip_left = 0;     // Serial.println("2");    }   //lfmin   if (x_axis < 100){     pos_foot_left = 0;     // Serial.println("4");    }   if (x_axis > 100 && x_axis < 800){     pos_foot_left = 75;   }    //lhmax   if (y_axis > 800){     pos_hip_left = 180;     // Serial.println("6");    }   //lfmax   if (x_axis > 800){     pos_foot_left = 180;     // Serial.println("8");    }   if (x_axis > 100 && x_axis < 800){     pos_foot_left = 75;   }   }   }  
